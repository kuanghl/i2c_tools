# limit cmakeminimum version 
cmake_minimum_required(VERSION 3.1)

# shell run .py
# execute_process(COMMAND python3 ${CMAKE_CURRENT_SOURCE_DIR}/setup.py install --prefix=${PREFIX})
# add_custom_target(install_py COMMAND python3 ${CMAKE_CURRENT_SOURCE_DIR}/setup.py install --prefix=${PREFIX})